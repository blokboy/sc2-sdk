"""SDK core context.

At this ticket's stage, this package holds only a raw python-sc2
connect-and-play script (`sdk.play`). The verified, semantic `bot.*` action/
observation API and the raw `sdk.*` passthrough layer described in the
project spec (https://github.com/blokboy/sc2-sdk/issues/1) are built in
ticket #3 (https://github.com/blokboy/sc2-sdk/issues/3), not here.
"""
